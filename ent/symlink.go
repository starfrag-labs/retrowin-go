// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/starfrag-lab/retrowin-go/ent/inode"
	"github.com/starfrag-lab/retrowin-go/ent/symlink"
)

// Symlink is the model entity for the Symlink schema.
type Symlink struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// InodeID holds the value of the "inode_id" field.
	InodeID int64 `json:"inode_id,omitempty"`
	// TargetPath holds the value of the "target_path" field.
	TargetPath string `json:"target_path,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SymlinkQuery when eager-loading is set.
	Edges        SymlinkEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SymlinkEdges holds the relations/edges for other nodes in the graph.
type SymlinkEdges struct {
	// Inode holds the value of the inode edge.
	Inode *Inode `json:"inode,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// InodeOrErr returns the Inode value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SymlinkEdges) InodeOrErr() (*Inode, error) {
	if e.Inode != nil {
		return e.Inode, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: inode.Label}
	}
	return nil, &NotLoadedError{edge: "inode"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Symlink) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case symlink.FieldID, symlink.FieldInodeID:
			values[i] = new(sql.NullInt64)
		case symlink.FieldTargetPath:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Symlink fields.
func (_m *Symlink) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case symlink.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case symlink.FieldInodeID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field inode_id", values[i])
			} else if value.Valid {
				_m.InodeID = value.Int64
			}
		case symlink.FieldTargetPath:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field target_path", values[i])
			} else if value.Valid {
				_m.TargetPath = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Symlink.
// This includes values selected through modifiers, order, etc.
func (_m *Symlink) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryInode queries the "inode" edge of the Symlink entity.
func (_m *Symlink) QueryInode() *InodeQuery {
	return NewSymlinkClient(_m.config).QueryInode(_m)
}

// Update returns a builder for updating this Symlink.
// Note that you need to call Symlink.Unwrap() before calling this method if this Symlink
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Symlink) Update() *SymlinkUpdateOne {
	return NewSymlinkClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Symlink entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Symlink) Unwrap() *Symlink {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Symlink is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Symlink) String() string {
	var builder strings.Builder
	builder.WriteString("Symlink(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("inode_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.InodeID))
	builder.WriteString(", ")
	builder.WriteString("target_path=")
	builder.WriteString(_m.TargetPath)
	builder.WriteByte(')')
	return builder.String()
}

// Symlinks is a parsable slice of Symlink.
type Symlinks []*Symlink
