// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/starfrag-lab/retrowin-go/ent/directoryentry"
	"github.com/starfrag-lab/retrowin-go/ent/inode"
)

// DirectoryEntryCreate is the builder for creating a DirectoryEntry entity.
type DirectoryEntryCreate struct {
	config
	mutation *DirectoryEntryMutation
	hooks    []Hook
}

// SetParentID sets the "parent_id" field.
func (_c *DirectoryEntryCreate) SetParentID(v int64) *DirectoryEntryCreate {
	_c.mutation.SetParentID(v)
	return _c
}

// SetName sets the "name" field.
func (_c *DirectoryEntryCreate) SetName(v string) *DirectoryEntryCreate {
	_c.mutation.SetName(v)
	return _c
}

// SetChildID sets the "child_id" field.
func (_c *DirectoryEntryCreate) SetChildID(v int64) *DirectoryEntryCreate {
	_c.mutation.SetChildID(v)
	return _c
}

// SetID sets the "id" field.
func (_c *DirectoryEntryCreate) SetID(v int64) *DirectoryEntryCreate {
	_c.mutation.SetID(v)
	return _c
}

// SetParent sets the "parent" edge to the Inode entity.
func (_c *DirectoryEntryCreate) SetParent(v *Inode) *DirectoryEntryCreate {
	return _c.SetParentID(v.ID)
}

// SetChild sets the "child" edge to the Inode entity.
func (_c *DirectoryEntryCreate) SetChild(v *Inode) *DirectoryEntryCreate {
	return _c.SetChildID(v.ID)
}

// Mutation returns the DirectoryEntryMutation object of the builder.
func (_c *DirectoryEntryCreate) Mutation() *DirectoryEntryMutation {
	return _c.mutation
}

// Save creates the DirectoryEntry in the database.
func (_c *DirectoryEntryCreate) Save(ctx context.Context) (*DirectoryEntry, error) {
	return withHooks(ctx, _c.sqlSave, _c.mutation, _c.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (_c *DirectoryEntryCreate) SaveX(ctx context.Context) *DirectoryEntry {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *DirectoryEntryCreate) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *DirectoryEntryCreate) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (_c *DirectoryEntryCreate) check() error {
	if _, ok := _c.mutation.ParentID(); !ok {
		return &ValidationError{Name: "parent_id", err: errors.New(`ent: missing required field "DirectoryEntry.parent_id"`)}
	}
	if _, ok := _c.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New(`ent: missing required field "DirectoryEntry.name"`)}
	}
	if v, ok := _c.mutation.Name(); ok {
		if err := directoryentry.NameValidator(v); err != nil {
			return &ValidationError{Name: "name", err: fmt.Errorf(`ent: validator failed for field "DirectoryEntry.name": %w`, err)}
		}
	}
	if _, ok := _c.mutation.ChildID(); !ok {
		return &ValidationError{Name: "child_id", err: errors.New(`ent: missing required field "DirectoryEntry.child_id"`)}
	}
	if len(_c.mutation.ParentIDs()) == 0 {
		return &ValidationError{Name: "parent", err: errors.New(`ent: missing required edge "DirectoryEntry.parent"`)}
	}
	if len(_c.mutation.ChildIDs()) == 0 {
		return &ValidationError{Name: "child", err: errors.New(`ent: missing required edge "DirectoryEntry.child"`)}
	}
	return nil
}

func (_c *DirectoryEntryCreate) sqlSave(ctx context.Context) (*DirectoryEntry, error) {
	if err := _c.check(); err != nil {
		return nil, err
	}
	_node, _spec := _c.createSpec()
	if err := sqlgraph.CreateNode(ctx, _c.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != _node.ID {
		id := _spec.ID.Value.(int64)
		_node.ID = int64(id)
	}
	_c.mutation.id = &_node.ID
	_c.mutation.done = true
	return _node, nil
}

func (_c *DirectoryEntryCreate) createSpec() (*DirectoryEntry, *sqlgraph.CreateSpec) {
	var (
		_node = &DirectoryEntry{config: _c.config}
		_spec = sqlgraph.NewCreateSpec(directoryentry.Table, sqlgraph.NewFieldSpec(directoryentry.FieldID, field.TypeInt64))
	)
	if id, ok := _c.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if value, ok := _c.mutation.Name(); ok {
		_spec.SetField(directoryentry.FieldName, field.TypeString, value)
		_node.Name = value
	}
	if nodes := _c.mutation.ParentIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   directoryentry.ParentTable,
			Columns: []string{directoryentry.ParentColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(inode.FieldID, field.TypeInt64),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.ParentID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := _c.mutation.ChildIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   directoryentry.ChildTable,
			Columns: []string{directoryentry.ChildColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(inode.FieldID, field.TypeInt64),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.ChildID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// DirectoryEntryCreateBulk is the builder for creating many DirectoryEntry entities in bulk.
type DirectoryEntryCreateBulk struct {
	config
	err      error
	builders []*DirectoryEntryCreate
}

// Save creates the DirectoryEntry entities in the database.
func (_c *DirectoryEntryCreateBulk) Save(ctx context.Context) ([]*DirectoryEntry, error) {
	if _c.err != nil {
		return nil, _c.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(_c.builders))
	nodes := make([]*DirectoryEntry, len(_c.builders))
	mutators := make([]Mutator, len(_c.builders))
	for i := range _c.builders {
		func(i int, root context.Context) {
			builder := _c.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*DirectoryEntryMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, _c.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, _c.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil && nodes[i].ID == 0 {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int64(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, _c.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (_c *DirectoryEntryCreateBulk) SaveX(ctx context.Context) []*DirectoryEntry {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *DirectoryEntryCreateBulk) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *DirectoryEntryCreateBulk) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}
