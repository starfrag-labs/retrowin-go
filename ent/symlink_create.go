// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/starfrag-lab/retrowin-go/ent/inode"
	"github.com/starfrag-lab/retrowin-go/ent/symlink"
)

// SymlinkCreate is the builder for creating a Symlink entity.
type SymlinkCreate struct {
	config
	mutation *SymlinkMutation
	hooks    []Hook
}

// SetInodeID sets the "inode_id" field.
func (_c *SymlinkCreate) SetInodeID(v int64) *SymlinkCreate {
	_c.mutation.SetInodeID(v)
	return _c
}

// SetTargetPath sets the "target_path" field.
func (_c *SymlinkCreate) SetTargetPath(v string) *SymlinkCreate {
	_c.mutation.SetTargetPath(v)
	return _c
}

// SetInode sets the "inode" edge to the Inode entity.
func (_c *SymlinkCreate) SetInode(v *Inode) *SymlinkCreate {
	return _c.SetInodeID(v.ID)
}

// Mutation returns the SymlinkMutation object of the builder.
func (_c *SymlinkCreate) Mutation() *SymlinkMutation {
	return _c.mutation
}

// Save creates the Symlink in the database.
func (_c *SymlinkCreate) Save(ctx context.Context) (*Symlink, error) {
	return withHooks(ctx, _c.sqlSave, _c.mutation, _c.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (_c *SymlinkCreate) SaveX(ctx context.Context) *Symlink {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *SymlinkCreate) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *SymlinkCreate) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (_c *SymlinkCreate) check() error {
	if _, ok := _c.mutation.InodeID(); !ok {
		return &ValidationError{Name: "inode_id", err: errors.New(`ent: missing required field "Symlink.inode_id"`)}
	}
	if _, ok := _c.mutation.TargetPath(); !ok {
		return &ValidationError{Name: "target_path", err: errors.New(`ent: missing required field "Symlink.target_path"`)}
	}
	if v, ok := _c.mutation.TargetPath(); ok {
		if err := symlink.TargetPathValidator(v); err != nil {
			return &ValidationError{Name: "target_path", err: fmt.Errorf(`ent: validator failed for field "Symlink.target_path": %w`, err)}
		}
	}
	if len(_c.mutation.InodeIDs()) == 0 {
		return &ValidationError{Name: "inode", err: errors.New(`ent: missing required edge "Symlink.inode"`)}
	}
	return nil
}

func (_c *SymlinkCreate) sqlSave(ctx context.Context) (*Symlink, error) {
	if err := _c.check(); err != nil {
		return nil, err
	}
	_node, _spec := _c.createSpec()
	if err := sqlgraph.CreateNode(ctx, _c.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	_c.mutation.id = &_node.ID
	_c.mutation.done = true
	return _node, nil
}

func (_c *SymlinkCreate) createSpec() (*Symlink, *sqlgraph.CreateSpec) {
	var (
		_node = &Symlink{config: _c.config}
		_spec = sqlgraph.NewCreateSpec(symlink.Table, sqlgraph.NewFieldSpec(symlink.FieldID, field.TypeInt))
	)
	if value, ok := _c.mutation.TargetPath(); ok {
		_spec.SetField(symlink.FieldTargetPath, field.TypeString, value)
		_node.TargetPath = value
	}
	if nodes := _c.mutation.InodeIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   symlink.InodeTable,
			Columns: []string{symlink.InodeColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(inode.FieldID, field.TypeInt64),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.InodeID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// SymlinkCreateBulk is the builder for creating many Symlink entities in bulk.
type SymlinkCreateBulk struct {
	config
	err      error
	builders []*SymlinkCreate
}

// Save creates the Symlink entities in the database.
func (_c *SymlinkCreateBulk) Save(ctx context.Context) ([]*Symlink, error) {
	if _c.err != nil {
		return nil, _c.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(_c.builders))
	nodes := make([]*Symlink, len(_c.builders))
	mutators := make([]Mutator, len(_c.builders))
	for i := range _c.builders {
		func(i int, root context.Context) {
			builder := _c.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*SymlinkMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, _c.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, _c.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, _c.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (_c *SymlinkCreateBulk) SaveX(ctx context.Context) []*Symlink {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *SymlinkCreateBulk) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *SymlinkCreateBulk) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}
