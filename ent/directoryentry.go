// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/starfrag-lab/retrowin-go/ent/directoryentry"
	"github.com/starfrag-lab/retrowin-go/ent/inode"
)

// DirectoryEntry is the model entity for the DirectoryEntry schema.
type DirectoryEntry struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// ParentID holds the value of the "parent_id" field.
	ParentID int64 `json:"parent_id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// ChildID holds the value of the "child_id" field.
	ChildID int64 `json:"child_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DirectoryEntryQuery when eager-loading is set.
	Edges         DirectoryEntryEdges `json:"edges"`
	inode_entries *int64
	selectValues  sql.SelectValues
}

// DirectoryEntryEdges holds the relations/edges for other nodes in the graph.
type DirectoryEntryEdges struct {
	// Parent holds the value of the parent edge.
	Parent *Inode `json:"parent,omitempty"`
	// Child holds the value of the child edge.
	Child *Inode `json:"child,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// ParentOrErr returns the Parent value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e DirectoryEntryEdges) ParentOrErr() (*Inode, error) {
	if e.Parent != nil {
		return e.Parent, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: inode.Label}
	}
	return nil, &NotLoadedError{edge: "parent"}
}

// ChildOrErr returns the Child value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e DirectoryEntryEdges) ChildOrErr() (*Inode, error) {
	if e.Child != nil {
		return e.Child, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: inode.Label}
	}
	return nil, &NotLoadedError{edge: "child"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DirectoryEntry) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case directoryentry.FieldID, directoryentry.FieldParentID, directoryentry.FieldChildID:
			values[i] = new(sql.NullInt64)
		case directoryentry.FieldName:
			values[i] = new(sql.NullString)
		case directoryentry.ForeignKeys[0]: // inode_entries
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DirectoryEntry fields.
func (_m *DirectoryEntry) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case directoryentry.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int64(value.Int64)
		case directoryentry.FieldParentID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field parent_id", values[i])
			} else if value.Valid {
				_m.ParentID = value.Int64
			}
		case directoryentry.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case directoryentry.FieldChildID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field child_id", values[i])
			} else if value.Valid {
				_m.ChildID = value.Int64
			}
		case directoryentry.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field inode_entries", value)
			} else if value.Valid {
				_m.inode_entries = new(int64)
				*_m.inode_entries = int64(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DirectoryEntry.
// This includes values selected through modifiers, order, etc.
func (_m *DirectoryEntry) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryParent queries the "parent" edge of the DirectoryEntry entity.
func (_m *DirectoryEntry) QueryParent() *InodeQuery {
	return NewDirectoryEntryClient(_m.config).QueryParent(_m)
}

// QueryChild queries the "child" edge of the DirectoryEntry entity.
func (_m *DirectoryEntry) QueryChild() *InodeQuery {
	return NewDirectoryEntryClient(_m.config).QueryChild(_m)
}

// Update returns a builder for updating this DirectoryEntry.
// Note that you need to call DirectoryEntry.Unwrap() before calling this method if this DirectoryEntry
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *DirectoryEntry) Update() *DirectoryEntryUpdateOne {
	return NewDirectoryEntryClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the DirectoryEntry entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *DirectoryEntry) Unwrap() *DirectoryEntry {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: DirectoryEntry is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *DirectoryEntry) String() string {
	var builder strings.Builder
	builder.WriteString("DirectoryEntry(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("parent_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ParentID))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("child_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ChildID))
	builder.WriteByte(')')
	return builder.String()
}

// DirectoryEntries is a parsable slice of DirectoryEntry.
type DirectoryEntries []*DirectoryEntry
