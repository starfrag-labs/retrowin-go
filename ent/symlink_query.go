// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/starfrag-lab/retrowin-go/ent/inode"
	"github.com/starfrag-lab/retrowin-go/ent/predicate"
	"github.com/starfrag-lab/retrowin-go/ent/symlink"
)

// SymlinkQuery is the builder for querying Symlink entities.
type SymlinkQuery struct {
	config
	ctx        *QueryContext
	order      []symlink.OrderOption
	inters     []Interceptor
	predicates []predicate.Symlink
	withInode  *InodeQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SymlinkQuery builder.
func (_q *SymlinkQuery) Where(ps ...predicate.Symlink) *SymlinkQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *SymlinkQuery) Limit(limit int) *SymlinkQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *SymlinkQuery) Offset(offset int) *SymlinkQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *SymlinkQuery) Unique(unique bool) *SymlinkQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *SymlinkQuery) Order(o ...symlink.OrderOption) *SymlinkQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryInode chains the current query on the "inode" edge.
func (_q *SymlinkQuery) QueryInode() *InodeQuery {
	query := (&InodeClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(symlink.Table, symlink.FieldID, selector),
			sqlgraph.To(inode.Table, inode.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, symlink.InodeTable, symlink.InodeColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Symlink entity from the query.
// Returns a *NotFoundError when no Symlink was found.
func (_q *SymlinkQuery) First(ctx context.Context) (*Symlink, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{symlink.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *SymlinkQuery) FirstX(ctx context.Context) *Symlink {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Symlink ID from the query.
// Returns a *NotFoundError when no Symlink ID was found.
func (_q *SymlinkQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{symlink.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *SymlinkQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Symlink entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Symlink entity is found.
// Returns a *NotFoundError when no Symlink entities are found.
func (_q *SymlinkQuery) Only(ctx context.Context) (*Symlink, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{symlink.Label}
	default:
		return nil, &NotSingularError{symlink.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *SymlinkQuery) OnlyX(ctx context.Context) *Symlink {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Symlink ID in the query.
// Returns a *NotSingularError when more than one Symlink ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *SymlinkQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{symlink.Label}
	default:
		err = &NotSingularError{symlink.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *SymlinkQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Symlinks.
func (_q *SymlinkQuery) All(ctx context.Context) ([]*Symlink, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Symlink, *SymlinkQuery]()
	return withInterceptors[[]*Symlink](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *SymlinkQuery) AllX(ctx context.Context) []*Symlink {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Symlink IDs.
func (_q *SymlinkQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(symlink.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *SymlinkQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *SymlinkQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*SymlinkQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *SymlinkQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *SymlinkQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *SymlinkQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SymlinkQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *SymlinkQuery) Clone() *SymlinkQuery {
	if _q == nil {
		return nil
	}
	return &SymlinkQuery{
		config:     _q.config,
		ctx:        _q.ctx.Clone(),
		order:      append([]symlink.OrderOption{}, _q.order...),
		inters:     append([]Interceptor{}, _q.inters...),
		predicates: append([]predicate.Symlink{}, _q.predicates...),
		withInode:  _q.withInode.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithInode tells the query-builder to eager-load the nodes that are connected to
// the "inode" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SymlinkQuery) WithInode(opts ...func(*InodeQuery)) *SymlinkQuery {
	query := (&InodeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withInode = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		InodeID int64 `json:"inode_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Symlink.Query().
//		GroupBy(symlink.FieldInodeID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *SymlinkQuery) GroupBy(field string, fields ...string) *SymlinkGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SymlinkGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = symlink.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		InodeID int64 `json:"inode_id,omitempty"`
//	}
//
//	client.Symlink.Query().
//		Select(symlink.FieldInodeID).
//		Scan(ctx, &v)
func (_q *SymlinkQuery) Select(fields ...string) *SymlinkSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &SymlinkSelect{SymlinkQuery: _q}
	sbuild.label = symlink.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SymlinkSelect configured with the given aggregations.
func (_q *SymlinkQuery) Aggregate(fns ...AggregateFunc) *SymlinkSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *SymlinkQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !symlink.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *SymlinkQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Symlink, error) {
	var (
		nodes       = []*Symlink{}
		_spec       = _q.querySpec()
		loadedTypes = [1]bool{
			_q.withInode != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Symlink).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Symlink{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withInode; query != nil {
		if err := _q.loadInode(ctx, query, nodes, nil,
			func(n *Symlink, e *Inode) { n.Edges.Inode = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *SymlinkQuery) loadInode(ctx context.Context, query *InodeQuery, nodes []*Symlink, init func(*Symlink), assign func(*Symlink, *Inode)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*Symlink)
	for i := range nodes {
		fk := nodes[i].InodeID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(inode.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "inode_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *SymlinkQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *SymlinkQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(symlink.Table, symlink.Columns, sqlgraph.NewFieldSpec(symlink.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, symlink.FieldID)
		for i := range fields {
			if fields[i] != symlink.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withInode != nil {
			_spec.Node.AddColumnOnce(symlink.FieldInodeID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *SymlinkQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(symlink.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = symlink.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SymlinkGroupBy is the group-by builder for Symlink entities.
type SymlinkGroupBy struct {
	selector
	build *SymlinkQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *SymlinkGroupBy) Aggregate(fns ...AggregateFunc) *SymlinkGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *SymlinkGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SymlinkQuery, *SymlinkGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *SymlinkGroupBy) sqlScan(ctx context.Context, root *SymlinkQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SymlinkSelect is the builder for selecting fields of Symlink entities.
type SymlinkSelect struct {
	*SymlinkQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *SymlinkSelect) Aggregate(fns ...AggregateFunc) *SymlinkSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *SymlinkSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SymlinkQuery, *SymlinkSelect](ctx, _s.SymlinkQuery, _s, _s.inters, v)
}

func (_s *SymlinkSelect) sqlScan(ctx context.Context, root *SymlinkQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
