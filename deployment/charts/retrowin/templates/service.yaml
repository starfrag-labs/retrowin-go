apiVersion: v1
kind: Service
metadata:
  name: {{ include "retrowin.fullname" . }}
  labels:
    {{- include "retrowin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "retrowin.selectorLabels" . | nindent 4 }}
